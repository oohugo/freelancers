<% if employer_signed_in? %>
	<h3><%= link_to 'Criar Projeto', new_project_path %></h3>
	<% unless @projects.empty? %>
		<ul>
		<% @projects.each do |project| %>
			<li><%= project.title %></li>
		<% end %>
		</ul>
	<% end %>
<% end %>

<% if worker_signed_in? %>
	<h3>Projetos disponiveis</h3>
	<% unless @projects.empty? %>
		<ul>
		<% @projects.each do |project| %>
			<li><%= project.title %></li>
		<% end %>
		</ul>
	<% end %>
<% end %>

